@using cs_record_shop_frontend.Models;

<RecordSummaryList albums="albums"></RecordSummaryList>

@code {
    // public List<Album> albums = new List<Album>{ 
    //     new Album(1, "First album", "This is the first album. TOTALLY RAD!", 1997, "First Artist"),
    //     new Album(2, "Second album", "This is the second album. STILL TOTALLY RAD!", 1998, "Second Artist"),
    //     new Album(3, "Thirs album", "This is the third album. Not so rad.", 1999, "Third Artist")};

    private List<Album>? albums = [];

    protected override async Task OnInitializedAsync()
    {
        using HttpClient httpClient = new HttpClient();
        albums = await httpClient.GetFromJsonAsync<List<Album>>("https://localhost:7223/albums");
    }
}
